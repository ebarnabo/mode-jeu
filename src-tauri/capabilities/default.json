{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions par défaut",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default"
  ]
}
